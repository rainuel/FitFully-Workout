{
  "name": "fitfully",
  "version": "1.0.0",
  "private": true,
  "description": "Fit Fully \u2014 offline Android workout tracker (vanilla JS + Capacitor + SQLite)",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "node --test \"tests/*.test.js\"",
    "sync": "npm run build && cap sync android",
    "android": "npm run build && cap sync android && cap run android",
    "studio": "npm run build && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "8.1.1",
    "@capacitor/android": "8.5.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "8.5.2",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "vite": "8.3.0"
  }
}
